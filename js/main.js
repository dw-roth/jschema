// 1. Use the dataSchema object in schema.js to get the json schema
// 2. Iterate through the json schema properties and add a lable and input field for each property in the schema. The input field should use
//    property name as the id. The label should use the description field
// 3. When the user fills out the form and clicks the submit button (yes, candidate needs to add a submit button), write a handler that takes the
//    input and calls dataObject.validate. If there is a validate error, display it to the user, otherwide indicate success.

